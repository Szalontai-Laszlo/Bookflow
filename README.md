# Library
# Ötlet: https://www.youtube.com/watch?v=zKguO4oaAGs
- ### angular
	- Go to project path angular folder
		```
		<Project Path>/angular/
		```
	- FIRST TIME !!! In the terminal window, type the command:
		```
		npm install
		```
	- START: In the terminal window, type the command:
		```
		ng serve
		```
